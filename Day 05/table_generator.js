function tableGenerartor(num,range){
    for(let i=1 ; i<=range ; i++){                 //The inputs you give inside the function are called function parameters
        console.log(`${num} x ${i} = ${num * i}`)
        
    }
}

tableGenerartor(2,50)
