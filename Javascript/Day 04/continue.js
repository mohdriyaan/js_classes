// Continue is Keyword in JS
// Continue is used to skip current iteration of loop of program
for (let i = 1; i <= 10; i++) {
    if (i >= 7) {
        console.log(`I am at ${i}`)
        continue
    }
}
