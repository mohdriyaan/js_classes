const a = [2,4,5,6,7,8]
const b={one:1,two:2}

//isArray()
// It checks whether the given array is array or not
// If it is Array , then it will be true else false
// Syntax:Array.isArray(variable)

let output=Array.isArray(a)
let output1=Array.isArray(b)
console.log(output,output1)
